name: Supply chain

'on':
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '17 3 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    uses: Tinkora/.github/.github/workflows/reusable-supply-chain.yml@e8958a100fd795846961299c46cba4d7583a19f5
    with:
      working-directory: .
      toolchain: stable
