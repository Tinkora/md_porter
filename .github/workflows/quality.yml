name: Quality

'on':
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    uses: Tinkora/.github/.github/workflows/reusable-rust-quality.yml@e8958a100fd795846961299c46cba4d7583a19f5
    with:
      working-directory: .
      toolchain: stable
      locked: true
      msrv: 1.95.0

  wasm:
    uses: Tinkora/.github/.github/workflows/reusable-wasm-quality.yml@e8958a100fd795846961299c46cba4d7583a19f5
    with:
      working-directory: crates/md_porter_web
      toolchain: stable
      locked: true
      playwright-smoke: false
